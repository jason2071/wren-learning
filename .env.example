# คัดลอกเป็น .env แล้วใส่ค่าจริง (อย่า commit .env)
#   cp .env.example .env
# โหลดเข้า shell ก่อนรัน wren:
#   export $(grep -v '^#' .env | xargs)

PG_HOST=127.0.0.1
PG_PORT=5432
PG_DATABASE=your_database
PG_USER=your_user
PG_PASSWORD=your_password
